<div class="border-perso">
    <p><b>Réinitialiser mon mot de passe</b></p>
    <p class="no-margin">Pour réinitialiser votre mot de passe utilisé lors de la connexion, saisissez deux fois votre nouveau choix et validez.</p>
</div>

<div style="width:70%;margin:0 auto 30px;border:1px solid #DDD;padding:10px;">
    <form action="{{ path('fos_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset form-horizontal" role="form">
        <div class="form-group">
            {{ form_label(form.plainPassword.first, 'Nouveau mot de passe :', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            <div class="col-sm-10">
                {{ form_widget(form.plainPassword.first, {'attr': {'class': 'form-control input-sm', 'placeholder' : 'Nouveau mot de passe', 'autocomplete' : 'off'}}) }}
            </div>
        </div>
        <div class="form-group">
            {{ form_label(form.plainPassword.second, 'Confirmation :', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            <div class="col-sm-10">
                {{ form_widget(form.plainPassword.second, {'attr': {'class': 'form-control input-sm', 'placeholder' : 'Confirmation', 'autocomplete' : 'off'}}) }}
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                {{ form_rest(form) }}
                <input type="submit" id="_submit" name="submit" value="Enregistrer" class="btn btn-success" />
            </div>
        </div>
    </form>
</div>